/**
 * Точка входа для хостинга серверных скриптов uCoz.
 *
 * Панель серверных скриптов запускает приложение через Passenger и по умолчанию ждёт
 * файл с именем app.js в корне приложения. Наш реальный сервер лежит в
 * src/web-server.mjs, поэтому здесь только переадресация на него.
 *
 * Файл намеренно на CommonJS-совместимом синтаксисе динамического импорта: так он
 * запустится независимо от того, как хостинг трактует расширение .js.
 *
 * Один процесс отдаёт сразу два интерфейса:
 *   /      витрина для людей, проверка сайта по URL
 *   /mcp   remote MCP для агентов, подключение одним адресом
 *
 * Переменные окружения задаются в панели, в разделе «Переменные окружения».
 * Рекомендуемый набор для боевого запуска:
 *   MCP_HOSTED=1                              скрыть тулы, которым нужен локальный Chrome
 *   TRUST_PROXY=1                             настоящий IP приходит от Passenger в заголовке
 *   MAX_PAGES=8                               потолок страниц в публичной проверке
 *   MCP_ALLOW_ORIGIN=https://seoaudit.ucoz.net  чей браузер может дергать /mcp
 * Порт передаёт сам Passenger через PORT, задавать его вручную не нужно.
 */
import('./src/web-server.mjs').catch((error) => {
  console.error('Не удалось запустить сервер:', error);
  process.exit(1);
});
